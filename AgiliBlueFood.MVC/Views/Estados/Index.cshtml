@model IEnumerable<AgiliBlueFood.MVC.ViewModels.EstadoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeEstado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SiglaEstado)
        </th>
        <th>
            @Html.DisplayName("Pais")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeEstado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiglaEstado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pais.NomePais)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.EstadoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.EstadoId }) |
            @Html.ActionLink("Apagar", "Delete", new { id=item.EstadoId })
        </td>
    </tr>
}

</table>
